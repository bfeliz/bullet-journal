{{!-- index --}}
<div class="row">
    <div class="col s12 m12 l6">

    </div>

    {{!-- todos new--}}
    <div class="col s12 m12 l6" id="daily-table">
        <div class="row">
            <div class="input-field col s12">
                <h1>New Task</h1>
                <textarea id="textarea1" class="materialize-textarea"></textarea>
                <a class="btn waves-effect waves-light newdaily" type="submit" name="action">Submit</a>

            </div>
            {{!-- render db --}}
        </div>

        <ol class='currentDailies'>Current Dailies</ol>
    </div>
</div>

{{!-- todos --}}