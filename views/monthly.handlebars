</div>
<div class="row monthly-row">
    <div class="col s12 m12 l6 monthly-columns">
        <h1 class="welcome">Hello</h1>
     
        <h3 class="month">{{months.month}}</h3>
      
        <div class='datepicker'>
            <div class="datepicker-header">
            </div>
        </div>
    </div>
    <div class="col l1"></div>

    <div class="col s12 m12 l5 monthly-columns" id="daily-table">
        {{#each datesParse}}
        <div class="row">
            <div class="input-field col s12">
               <label for="first_name">{{date}}</label>
                <textarea id="{{id}}" class="materialize-textarea">{{value}}</textarea>
            </div>
        </div>
        {{/each}}
    </div>
</div>
<div class="container">
  